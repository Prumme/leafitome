DATABASE_URL=postgres://leafitome:leafitome@localhost:5433/leafitome
PORT=3001
JWT_SECRET=dev-leafitome-change-me-in-production
CORS_ORIGIN=http://localhost:5173

# Web Push (générer : npx web-push generate-vapid-keys)
VAPID_PUBLIC_KEY=
VAPID_PRIVATE_KEY=
VAPID_SUBJECT=mailto:hello@leafitome.local

# Dashboard /admin (session 2h) — laisser vide pour désactiver
ADMIN_PASSWORD=change-moi-admin

# Emails (Resend) — laisser RESEND_API_KEY vide en local pour désactiver l’envoi
RESEND_API_KEY=
EMAIL_FROM=Leafitome <onboarding@resend.dev>
APP_URL=http://localhost:5173
